# CI AMQPS mTLS phase 1: verify_peer only. Do not list EXTERNAL until
# rabbitmq_auth_mechanism_ssl is enabled (otherwise prelaunch fails).
listeners.tcp.default = 5672
listeners.ssl.default = 5671

ssl_options.cacertfile = /certs/ca.pem
ssl_options.certfile   = /certs/server.pem
ssl_options.keyfile    = /certs/server.key
ssl_options.verify     = verify_peer
ssl_options.fail_if_no_peer_cert = true
