# Lean-only mesh

```bash
lake exe lean_mesh_service    # terminal 1
lake exe lean_mesh_client     # terminal 2
```

Identity `demo` on `nr.mesh` (same contract as `examples/one_client_one_service`).
